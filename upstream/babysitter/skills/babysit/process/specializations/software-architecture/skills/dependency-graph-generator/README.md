# Dependency Graph Generator

Generates module dependency graphs with circular dependency detection and coupling metrics.

## Features

- Dependency graph generation
- Circular dependency detection
- Coupling metrics calculation
- Multiple output formats (DOT, D3, Mermaid)
- Package-level grouping

## Usage

Provide entry points and output format. Generates dependency visualization with metrics.
