# GraphQL Schema Generator

Generates GraphQL SDL schemas from data models with resolver stubs and federation support.

## Features

- SDL generation from data models
- Type inference
- Resolver stub generation
- Apollo Federation support
- Input and mutation type generation

## Usage

Provide data models with fields and relationships. Generates complete GraphQL schema with optional resolvers.
