# Kubernetes Manifest Validator

Validates Kubernetes manifests for security, best practices, and resource limits.

## Features

- Manifest validation (kubeval)
- Security policy checking (OPA, kubesec)
- Best practice linting (kube-linter)
- Resource limit validation
- RBAC and network policy analysis

## Usage

Provide manifest paths and select validators. Returns validation results and security findings.
