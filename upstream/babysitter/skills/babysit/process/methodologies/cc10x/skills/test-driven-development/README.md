# Test-Driven Development Skill

Strict RED-GREEN-REFACTOR enforcement. Tests first, always. Run mode only, never watch mode.

## Cycle

RED (exit 1) -> GREEN (exit 0) -> REFACTOR (exit 0)

## Invocation

Used by: `component-builder`, `bug-investigator` agents
